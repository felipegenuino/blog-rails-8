<hr class="my-10">

<section class="max-w-2xl mx-auto mt-12 pt-12 border-t border-gray-200">
  <h3 class="text-xl font-bold mb-6">Comentários (<%= @post.comments.count %>)</h3>
  
  <div id="comments">
    <%= render @post.comments %>
  </div>

  <%= render "comments/form" %>
</section>