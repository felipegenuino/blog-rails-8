<%# Adiciona o novo comentário no topo da lista sem refresh %>
<%= turbo_stream.prepend "comments", partial: "comments/comment", locals: { comment: @comment } %>

<%# Limpa o formulário após enviar %>
<%= turbo_stream.replace "new_comment", partial: "comments/form", locals: { post: @post } %>