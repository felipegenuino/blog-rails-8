<nav class="bg-white border-b border-gray-200 sticky top-0 z-50">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 justify-between items-center">
      <div class="flex items-center gap-8">
        <%= link_to "Meu Blog", root_path, class: "text-2xl font-black tracking-tighter text-indigo-600 hover:opacity-80 transition-opacity" %>
        <div class="hidden md:flex items-center space-x-4">
          <%= link_to "Posts", posts_path, class: "text-sm font-medium text-gray-600 hover:text-indigo-600" %>
        </div>
      </div>

      <div class="flex items-center gap-4">
        <% if authenticated? %>
          <div class="flex items-center gap-x-4 border-l pl-4 border-gray-200">
            
            <% if authenticated? && Current.user.admin? %>
              <%= link_to admin_dashboard_path, class: "flex items-center gap-2 p-2 hover:bg-gray-100 rounded-lg" do %>
                <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
                <span>Admin Dash</span>
              <% end %>
            <% end %>

            <%= button_to session_path, method: :delete, 
                class: "flex items-center gap-x-2 rounded-full bg-red-50 px-3 py-1.5 text-xs font-bold text-red-600 hover:bg-red-100 transition-all border-none cursor-pointer",
                data: { turbo: false } do %>
              <i data-lucide="log-out" class="w-3.5 h-3.5"></i>
              Sair
            <% end %>
          </div>
        <% else %>
          <%= link_to "Entrar", new_session_path, class: "text-sm font-semibold text-gray-900 hover:text-indigo-600" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>